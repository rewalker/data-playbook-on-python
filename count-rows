import pandas as pd

# Load the dataset using the provided file path
file_path = r"#set the file path"
data = pd.read_csv(file_path)

# Count the occurrences where Classes = 'fire'
fire_count = data[data['Classes'] == 'fire'].shape[0]

# Count the total number of data lines
total_data_lines = data.shape[0]

print(f"The number of times Classes = 'fire' is: {fire_count}")
print(f"The total number of data lines is: {total_data_lines}")



#This is a python example that counts number of all lines in an dataset. 
#Also we see an example on counting when a parameter ise set.




  

